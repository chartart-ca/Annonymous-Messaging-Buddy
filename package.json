{
  "name": "anonymous-message-hub",
  "version": "1.0.0",
  "description": "A web application for sending and receiving anonymous messages with SQLite backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p public && cp -v index.html public/ || echo 'Error: Failed to copy index.html to public/'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "license": "MIT"
}
